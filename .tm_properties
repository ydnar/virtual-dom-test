# Settings
tabSize              = 2
projectDirectory     = "$CWD"
windowTitle          = "$TM_DISPLAYNAME — ${CWD/^.*\///}"

# Ignores
extraExcludes = "dist,node_modules,log,vendor,tmp,output,.sass-cache,.DS_Store,"
excludeInFileChooser  = "{$excludeInFileChooser,$extraExcludes}"
excludeInFolderSearch = "{$excludeInFolderSearch,$extraExcludes}"

# Variables
TM_ORGANIZATION_NAME = 'nb.io, LLC'
